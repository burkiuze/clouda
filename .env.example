# Postgres. Two URLs: the pooled one the app uses at runtime, and a direct
# one that migrations need (pgbouncer cannot run DDL in transaction mode).
# Supabase: Project Settings -> Database -> Connection string.
DATABASE_URL="postgresql://postgres.<ref>:<password>@aws-0-<region>.pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=1"
DIRECT_URL="postgresql://postgres:<password>@db.<ref>.supabase.co:5432/postgres"

# Auth.js
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=""
# Your deployed URL, e.g. https://clouda.vercel.app (not needed on Vercel prod,
# it is inferred, but useful for local dev)
NEXTAUTH_URL="http://localhost:3000"

# Google OAuth (Google Cloud Console -> APIs & Services -> Credentials)
# Authorized redirect URI must be: https://<your-domain>/api/auth/callback/google
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# Credits granted to every new account on signup
SIGNUP_FREE_CREDITS=2000

# Search provider key (pick ONE). Without a key the engine still answers, but
# only from open sources (Wikipedia, Google News) — cloud IP ranges are blocked
# by every free general-web source. Whichever key is set is used first.
# Tavily: https://tavily.com  |  Brave: https://brave.com/search/api
# Serper: https://serper.dev
TAVILY_API_KEY=""
BRAVE_SEARCH_API_KEY=""
SERPER_API_KEY=""

# Optional: raises the GitHub provider's rate limit for code/repo results.
GITHUB_TOKEN=""

# Protects the scheduled monitor sweep at /api/cron/monitors.
CRON_SECRET=""
